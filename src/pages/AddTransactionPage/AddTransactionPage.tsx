import TransactionForm from '../../components/TransactionForm/TransactionForm';

export default function AddTransactionPage() {
  return (
    <div>
        <h2 className='main-title'>Новая транзакция</h2>

        <TransactionForm />
    </div>
  )
}
